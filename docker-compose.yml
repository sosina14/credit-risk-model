version: "3.8"
services:
  credit-risk-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    environment:
      - MODEL_PATH=models/best_model.pkl
    restart: unless-stopped